package com.aws.lambda;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class LambdaAwsSampleApplicationTests {
	
	
	/*
	 * @Autowired RestTemplate restTemplate;
	 * 
	 * @Test public void testLambdaFunctionHandler() {
	 * 
	 * MethodHandler handler = new MethodHandler(); String input = "test"; String
	 * result = restTemplate.getForObject("http://headers.jsontest.com/",
	 * String.class); String output = handler.apply(input); if (output != null) {
	 * System.out.println(output.toString()); } Assert.assertEquals("test Batman " +
	 * result.toString(), output); }
	 */

}
